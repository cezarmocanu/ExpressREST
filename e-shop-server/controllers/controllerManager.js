const usersController = require("./usersController");
const categoryController = require("./categoryController");

const controllers = {
  usersController,
  categoryController,
};

module.exports = {
  controllers,
};
